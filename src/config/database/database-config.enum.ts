export enum DatabaseConfiguration {
  HOST = 'host',
  USERNAME = 'username',
  PASSWORD = 'password',
  PORT = 'port',
  DATABASE = 'database',
  AUTOLOAD_ENTITIES = 'autoLoadEntities',
  SYNCHRONIZE = 'synchronize',
}
